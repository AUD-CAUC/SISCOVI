import {Rubrica} from '../rubricas/rubrica';

export class Percentual {
  nome: string;
  percentual: number;
  dataInicio: Date;
  dataAditamento: Date;
  dataFim: Date;
  codigo: number;
  rubrica: Rubrica;
}
